# 测试策略

状态：pytest、Vitest、生产构建，以及隔离的视频导入和采样帧真实链路冒烟已启用；手动/自动标注已具备单元、组件和 API/Worker 集成测试，真实模型与导出端到端仍待补充。

## 遗留测试基线

`dataset-manager-1` 的前后端包清单没有正式测试脚本。仓库中存在 yt-dlp、缩略图和抽帧格式的临时脚本以及历史测试输出，但不能证明当前提交的正确性。

已验证前端生产构建可成功完成，但主 JavaScript chunk 约 1.28 MB，并有大 chunk 警告。构建成功只证明可编译，不代替行为测试。

## 测试层级

### 单元测试

优先覆盖无 I/O 业务规则：

- 视频状态的合法和非法转移。
- 三种采样模式、边界时长、FPS 和参数限制。
- train/val 视频级划分与目标比例偏差。
- 帧启停、分组与导出资格推导。
- 路径解析、ID 和配置校验。
- multi/single 运行模式、注册审批和项目角色权限。
- 项目标签名称、中文描述、颜色校验、排序、启停、版本冲突和项目内唯一性。
- GPU 命令、PyTorch CUDA、ONNX CUDA Provider、Ultralytics 与 Transformers 依赖探测的成功和降级路径。
- 标注草稿历史、矩形边界、视图坐标变换、追加/覆盖合并和冲突判断。
- 筛帧跨页范围选择、启停模板顺序和基准/草稿差异计算。

### 数据与迁移测试

- 在空数据库上执行全部迁移。
- 从每个受支持的前一版本升级。
- 对真实 schema 的脱敏样本执行遗留项目导入预览。
- 验证外键、唯一约束、任务领取竞争和租约过期。
- 验证视频短码格式、项目内唯一、跨项目可重复、碰撞重试、迁移回填和视频上限触发器在 SQLite 表重建后仍存在。
- 验证单项目第 999 条视频成功、第 1000 条由数据库拒绝，且其他项目不受影响。
- 验证项目标签外键、项目内名称唯一、项目删除级联、中文描述迁移回填和已引用标签拒删。
- 验证标注删帧级联、标签删除受限、帧修订号、稳定显示顺序回填，以及推理模型和新增任务类型迁移。
- 迁移失败必须回滚或留下可诊断状态。

### API 集成测试

- 项目、视频、帧、标注、模型、自动标注、导出和任务的正常路径。
- 标签 owner/editor 写、viewer 只读、非成员 404，以及已认证能力接口。
- 参数错误、资源不存在、冲突、权限不足和部分失败。
- 分页、过滤、排序、幂等键和乐观并发。
- 标注帧摘要的成员读取，以及混合启停单事务写入、重复帧 ID、viewer 拒写和帧修订冲突。
- 文件下载的 Range、路径安全与内容类型。
- 事件断线重连后能从任务资源恢复最终状态。

### Worker 与媒体集成测试

- 用短小、仓库可生成的测试视频验证 ffprobe 和 FFmpeg。
- yt-dlp 网络行为默认使用假适配器；少量真实网络冒烟测试单独运行，不作为普通单元测试前提。
- 验证任务成功、超时、进程终止、重试、取消和重复领取。
- 验证 FFmpeg stdout/stderr 持续消费、线程限制和结构化进度。
- 验证临时输出发布、磁盘写失败和数据库提交失败后的清理/补偿。
- 验证原视频、缩略图、帧目录和帧文件使用项目内视频短码，帧平铺复制不依赖子目录避免重名。
- 验证模型文件/目录入库、符号链接拒绝、单张假推理映射，以及批量逐帧提交、取消和部分成功保留。

### 前端测试

- 组件测试覆盖表单、状态、错误、空数据和权限差异。
- API 客户端使用契约样本测试序列化和错误映射。
- 端到端测试覆盖创建项目到导出数据集的主路径。
- 批量操作测试请求数量，防止恢复逐帧请求瀑布。
- 关键工作区做键盘操作和基础可访问性检查。
- 批量抽帧执行期间验证页面仍可操作，进度只更新相关行和任务抽屉。
- 验证视频列表 25/50/100/200/全部分页、当前页全选/半选、999 容量禁用和 viewer 只读启停状态。
- 验证状态信息优先级覆盖活动任务、新失败/取消、停用、筛帧、恢复全部、已采样、待抽帧和基础媒体状态。
- 验证停用视频仍可播放、配置采样、抽帧和管理帧；未来下游功能另测 `enabled=true` 过滤。
- `AppShell.spec.ts` 覆盖全局导航、权限入口、侧栏偏好、最近项目上限、固定图标轨道和浮层账号菜单命令。
- `ProjectLabelsView.spec.ts` 覆盖双语紧凑新增、0 起始顺序、启停、原子排序、viewer 只读、候选颜色会话恢复及成功/失败后的换色规则；`labelColor.spec.ts` 覆盖高饱和随机颜色和已有色相避让；`AppShell.spec.ts` 另覆盖 GPU 降级提示每浏览器会话只显示一次。
- `ProjectLayout.spec.ts` 覆盖项目加载、紧凑上下文栏和真实路由页签；内外两层路由过渡不得重建整个项目外壳。
- `TaskCenterDrawer.spec.ts` 使用 fake timer 覆盖终态未读判断、已读清除、项目级重试、interval 清理和 append-to-body 浮层渲染。
- 使用 Element Plus `append-to-body` 的组件测试必须从 `document.body` 查询浮层内容，并在用例结束后清理；不得为了测试便利退回会受局部 overflow 影响的内联浮层。
- `base.spec.ts` 锁定 Element Plus 全局过渡时长、页面进入位移和 `prefers-reduced-motion` 降级，防止全局样式覆盖再次削弱组件动效。
- 组件测试验证尺寸令牌和固定轨道的结构约束；字体观感、动效节奏、浮层定位及 100% 浏览器缩放下的页面一致性仍需浏览器目测，当前没有像素级视觉回归测试。
- `recentProjects.spec.ts` 覆盖损坏的 localStorage、去重、当前项目优先和 5 项上限。
- 标注组件测试覆盖焦点路由、viewer 禁用入口、Konva 绘制/选择/变换、点击/Space 拖拽模式复位、撤销/重做、快捷键输入保护与 `S/Y/L/H/P`、切帧/关闭保存、图像信息折叠、框外全局对象序号与模型置信度、对象级显隐/删除、统一缩略时间与启停状态、16:9 缩略图坐标映射、SVG 缩略视口和缩略图导航。
- 自动标注组件测试覆盖模型登记、自由英文类别与 `All` 互斥、单张草稿、追加/覆盖参数、批量二次确认、保存遮罩、批量只读状态和任务轮询。
- 筛帧组件测试覆盖默认 100 条分页、全量元数据及标注预览、默认开启的内存显隐开关、标注帧启停统计、viewer 只读大图、网格/大图/标注图像信息的短码文件名、启停草稿不即时写库、差异原子保存、`S/H` 大图快捷键，以及框外类别/序号/模型置信度、缩放拖拽和缩略视口。

## 必测工作流

1. 创建项目并导入本地视频。
2. 远程视频下载失败后重试成功。
3. 计算采样、抽帧、重启服务并恢复任务结果。
4. 筛选部分帧、分组并导出 train/val。
5. 两个客户端并发修改同一资源时检测冲突。
6. 路径逃逸、越权访问和跨项目任务订阅被拒绝。
7. 手动标注、直接模型自动标注、人工复核和冲突报告。
8. Docker 未声明 GPU 时正常启动并显示功能降级。
9. 停机复制工作区后在 Linux 或 Windows 重新定位并启动。

图片数据集和数据集导出测试在对应功能进入实现范围时补充；真实 GPU 模型文件、长视频批量性能和浏览器视觉回归仍需单独冒烟。

## Fixtures

- 媒体 fixture 应尽量用测试过程生成，体积小且许可明确。
- 数据库 fixture 通过迁移或工厂创建，不长期保存来源不明的二进制数据库。
- 遗留兼容 fixture 放在专门测试资源中并脱敏，不直接依赖 `.ai-local/references/`。
- 测试目录必须隔离，完成后只删除由该测试显式创建的路径。

## 质量门槛

- 每个缺陷修复先有可复现测试。
- 新状态转移、迁移和权限规则必须有正反例。
- 覆盖率数字不是唯一门槛；核心领域分支和失败路径必须显式枚举。
- 合并前至少运行格式、静态检查、单元、集成和前端构建。

后端使用 pytest，前端单元/组件使用 Vitest。当前可执行命令：

```bash
cd backend && uv run pytest
cd backend && uv run ruff check src tests alembic
cd frontend && npm test
cd frontend && npm run build
```

当前后端 126 项测试覆盖健康检查、路径逃逸、工作区定位、一次性口令、十二版迁移、初始化回滚、认证会话生命周期、模式切换、注册审批、项目权限、项目标签、视频短码与受管文件命名、标注外键、稳定顺序与修订冲突、推理模型、单张/批量自动标注、GPU/模型运行时能力、全局任务可见性、媒体解析、视频导入、单项目 999 容量、视频启停与版本冲突、三种采样算法、采样/帧 API、任务租约/并发/取消/重试、抽帧发布与重采样保护。前端 91 项测试覆盖路由、全局应用框架、最近项目、初始化与认证、用户/项目/标签管理、高密度视频工作台、视频短码显示、视频状态和分页、本地文件/目录直接导入、采样与全屏筛帧、标签颜色、GPU 降级、任务中心，以及在线标注历史、画布、快捷键、全局对象序号、缩略图预览、保存遮罩、工作台、全帧网格不可压缩结构、模型登记和自动标注交互。

2026-07-28 在本地 NVMe、Python 3.12、SQLite 3.50.4 rollback journal 环境执行了一次临时容量诊断：初始生成 100 万 Frame 和每帧 5 个 Annotation，数据库在两个视频各重建 2 万 Frame 后包含 480 万 Annotation、约 794.2 MiB。热缓存中，单视频 2 万帧计数中位数 0.374 ms、末页 200 条 0.627 ms、启用帧 200 条 0.173 ms、按 Frame 主键读取 0.005 ms、单帧标注计数 0.004 ms；查询计划分别使用 `(video_id, sequence)`、Frame 主键和 `annotations.frame_id`，未依赖已删除的单列 Frame 索引。两个发布形态事务在 0.315 s 和 0.554 s 内串行完成，并发读取 0.002 s 完成，未发生锁错误。该结果只证明本机合成元数据路径，未包含真实图片写入或生产硬件，不能作为部署 SLA。

GPU 实机冒烟此前已在本项目 uv `gpu` extra 中验证：PyTorch 2.9.1+cu128 可识别 RTX A4000 与 Quadro RTX 4000 并执行 CUDA 张量运算；ONNX Runtime 1.26.0 暴露 TensorRT、CUDA 和 CPU Provider；Ultralytics 可导入。当前在线标注实现使用假推理器覆盖契约，本次尚未用用户实际 YOLO/GroundingDINO 权重执行端到端推理。CI 仍不要求具备 GPU。

项目权限还通过隔离工作区中的真实 Uvicorn 进程冒烟验证：多用户隔离、editor 修改、viewer 拒写、过期版本 409、多用户管理员不越权，以及重启到 single 模式后的管理员等效 owner 访问均通过。

媒体链路使用测试时生成的短 MP4、真实 ffmpeg/ffprobe、真实 yt-dlp、局域网 HTTP 服务、Uvicorn 和独立 Worker 冒烟验证：目录一级扫描、本地复制、远程解析/下载、取消后重试、API 并行可响应、持久任务成功、viewer Range 播放/下载与拒绝导入，以及跨项目 404 隔离均通过。该脚本位于本地 `.ai-local/dev/`，不是 CI 或运行时依赖。不先设置脱离实际风险的覆盖率数字。

采样链路另用 120 帧测试视频、真实 FFmpeg、Uvicorn 和独立 Worker 验证：目标帧数模式精确生成 50 张 JPG、固定间隔模式生成 12 张 PNG、重采样原子替换上一代目录、原视频/缩略图/帧目录及帧文件均按响应中的视频短码命名、帧批量禁用、过期 `frame_revision` 返回 409，以及 viewer 可读图片但写操作返回 403。冒烟脚本同样位于本地 `.ai-local/dev/`，不作为运行时依赖。
